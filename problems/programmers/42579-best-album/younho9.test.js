const solution = require('./younho9');

describe('42579-best-album', () => {
    it('test 1', () => {
        const input = '';
        const result = '';
        expect(solution(input)).toEqual(result);
    });
});
